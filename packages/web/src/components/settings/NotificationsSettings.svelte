<script lang="ts">import {alertPermission,requestAlertPermission} from '../../lib/alerts';</script>
<div class="settings-section"><div class="row"><div class="row-main"><div class="row-label">Alert me when a todo is due</div><div class="row-desc">Your browser shows a notification when Inbox RS is open.</div></div><div class="row-ctl"><span class="pill" class:ok={$alertPermission==='granted'} class:warn={$alertPermission==='denied'}>{$alertPermission==='granted'?'Allowed':$alertPermission==='unsupported'?'Unsupported':'Not allowed'}</span><button type="button" class="switch" class:on={$alertPermission==='granted'} aria-label="Alert me when a todo is due" aria-pressed={$alertPermission==='granted'} onclick={()=>void requestAlertPermission()}></button></div></div></div>
